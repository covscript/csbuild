import csbuild
function csbuild_main(config, args)
    config.name = "csbuild_test"
    config.author = "kiva"
    config.version = "1.0"
    config.version_num = 1

function csbuild_main(config)
    config.name = "csbuild_test"
    config.author = "Kiva"
    config.version = "1.0"
    config.version_code = 1

    @begin
    config.target("test1", {"1.cpp"})
        .link("m")
        .link_linux("pthread")
        .link_darwin("pthread-darwin")
        .link_windows("windows-thread")
        .compile_options("-Wall")
        .compile_options_windows("/fuckmsvc")
        .compile_options_linux("-fuckgcc")
        .compile_options_darwin("-fuckclang")
    @end

    @begin
    config.target("test2", {"2.cpp"})
        .link("m")
        .link_linux("pthread")
        .link_darwin("pthread-darwin")
        .link_windows("windows-thread")
        .compile_options("-Wall")
        .compile_options_windows("/fuckmsvc")
        .compile_options_linux("-fuckgcc")
        .compile_options_darwin("-fuckclang")
    @end
end
